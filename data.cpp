#include "data.h"

data::data()
{

}


/*vector <QPair> GetSet() const {
        vector <pair> aux;
        aux.size() = set.size();
        for (vector<pair>::const_iterator it=set.begin();
             it != set.end(); ++it) {
            aux.first = set.first;
            aux.second = set.second;
        }
        return aux;
    }*/
